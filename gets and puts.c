#include<stdio.h>
#include<conio.h>
#include<string.h>
int main()
{
    char string[23];
    printf("Enter the string\n");
    gets(string);
    while(string[i]!='\0')
      i++;
      return 0;

}